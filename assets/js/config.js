/* ITI Exam Prep — site config (safe to commit; no secrets) */
window.ITI_SITE_CONFIG = {
  /* Paste your GA4 Measurement ID from analytics.google.com, e.g. "G-XXXXXXXXXX".
     Leave empty to skip loading Google Analytics (no errors). */
  gaMeasurementId: "G-PD85H1KTQZ"
};
/* Optional override: window.ITI_GA_ID = "G-XXXXXXXXXX"; takes precedence if set. */
